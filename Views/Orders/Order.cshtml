@model lsad.Models.SepeteEkle

@{
    ViewBag.Title = "Order";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Order</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>SepeteEkle</h4>
        <div>
            @{ 
                if(ViewBag.Result!= null) {
                    <h3>@ViewBag.Result</h3>

                     }


            }
        </div>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.Label("PhoneNumber", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextArea("PhoneNumber", new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessage( "PhoneNumber", "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Adress", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextArea("Adress", new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessage("Adress", "", new { @class = "text-danger" })
            </div>
        </div>

      

  

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Confirm" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
